<header id="header-section" class="structural-col mb-4">
   <nav class="navbar navbar-expand-lg navbar-light">
    <div class="container-fluid">
          <a href="{{ path('app_home') }}" class="brand-name navbar-brand fw-bold"><img src="{{ asset('/assets/img/logo_blog.webp') }}" width="100"></a>
          <div class="links-container collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
              <li class="link nav-item"><a class="nav-link active" href="{{ path('app_home') }}">Accueil</a></li>
              <li class="link nav-item"><a class="nav-link active" href="{{ path('app_article') }}">Articles</a></li>
              <li class="link nav-item"><a class="nav-link active" href="{{ path('app_article_create') }}">Publication</a></li>
            </ul>
          </div>
              {% block subnav %}
        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
          <div class="d-flex flex-row justify-content-end pe-3">
            <a href="{{ path('app_logout') }}" class="connect btn btn-outline-light ms-3 fw-bold">Déconnexion</a>
          </div>
        {% else %}
          <div class="d-flex flex-row justify-content-end pe-3">
            <a href="{{ path('app_login') }}" class="connect btn btn-outline-light ms-3  fw-bold">Connexion</a>
            <a href="{{ path('app_register') }}" class="connect btn btn-outline-light ms-3  fw-bold">Inscription</a>
          </div>
        {% endif %}
      {% endblock %}
    </div>
           
    </nav>
</header>
    